# ====== .env.local (para rodar direto no WSL, sem Docker) ======
# Ajuste host/porta conforme seu Postgres local
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/posts_db

PORT=5000
JWT_SECRET=devsecret_change_me
